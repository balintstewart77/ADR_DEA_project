# Post-registration outputs

This area is for official draw logs, restricted formal REDCap exports,
preregistration amendments, final results, and adjudication outputs created only
after registration and the relevant protocol step. The official draw was
executed exactly once on 24 July 2026; its aggregate execution record is
`official_draw_logs/official_draw_execution_20260724.md`. Deterministic formal
assignments were generated later from the 225 active records only; the
aggregate-only record is
`official_draw_logs/formal_assignment_generation_20260724.md`. Those frozen
assignments were subsequently imported into REDCap using the manual Data Import
Tool; the administrative import record is
`redcap_import_logs/formal_assignment_import_20260724.md`. That import created
675 new records (225 per coder) and left the 35 existing pilot/QA records
untouched, for 710 records total; access separation is enforced by Data Access
Groups, not dashboard membership. Restricted-user live QA was not performed, and
first-login confirmation from each coder is required before coding begins.
Formal validation coding has not started, and reserve activation and Project
Owner recruitment remain unstarted. Other empty tracked directories and
`.gitkeep` files remain structural placeholders only and do not mean that
coding, analysis, adjudication, or release has occurred.

Do not place reserve manifests or personal data in a public path. REDCap
exports remain ignored and require the approved restricted-data workflow.
The active and reserve draw files remain ignored under
`preregistration_restricted/sampling/official_draw_20260724/`; the tracked log
and canonical receipt expose only aggregate counts and byte provenance. The
restricted assignment crosswalk and generated REDCap import remain ignored
under `preregistration_restricted/assignments/formal_validation_20260724/`;
neither has been imported into REDCap, and reserve records were not assigned.
