# Source and cleaning

This folder is for the exact source-register snapshot, source manifest,
cleaning/versioning note, cleaning code, reviewed duplicate rulings, stable
Record-ID procedure, and a standalone frozen 1,308-row cleaned register. It must
not contain raw credentials, local QA scratch files, or an official validation
sample. These are pre-existing materials expected to be frozen at registration.
The source, code, rulings, and frozen 1,308-row cleaned register now exist.
`dea_accredited_projects_20260601_cleaned_1308.csv`,
`register_cleaning_integrity_report.json`,
`record_id_whitespace_normalisation_audit.csv`, and
`record_id_whitespace_migration_log.json`, and
`record_id_normalisation_verification_report.md` document the deterministic
pre-registration correction that strips boundary whitespace from `Record ID`.
The raw June source snapshot remains unchanged. No official validation sample
is contained in this folder or was generated by this correction.

Phase 3 source and cleaning provenance is complete. The machine-readable
`source_register_provenance.json` records the publisher, source URL, snapshot
and retrieval dates, protected raw-source hash, documented LF/CRLF hash
relationship, deterministic cleaning inputs, and frozen cleaned-population
identity. The source, cleaning code, reviewed rulings and 1,308-record cleaned
population are frozen authoritative Phase 3 artefacts.
