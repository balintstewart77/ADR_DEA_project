# Exclusions and sampling

This folder is for the final v8 list of 22 unique excluded stable Record IDs,
the frozen official sampling script and tests, and a blank execution checklist.
It must not contain active or reserve sample manifests, executed draw logs, or
reserve IDs. Exclusions and sampling code are expected to be frozen; the
checklist is a registration-time template. The v7 file is superseded by design,
and neither v7 nor v8 nor official validation sampling code was located. The
future formal seed is `SEED_DRAW = 20260713`; it has not been executed.
